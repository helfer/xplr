
* store formatted_address on server
* on client, do a position lookup for the formatted_address and then use that coordinate
    (that way, I hope we won't end up in stores)
* display more 'places' results on the client side. fetch them with nearby search.
